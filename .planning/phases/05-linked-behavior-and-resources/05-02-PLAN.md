---
phase: 05
plan: 02
title: Clean up textures and remove Daipai effect from foot melee hits
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/kotlin/com/columbina/yujie/item/BigSweatyFootItem.kt
  - src/main/resources/assets/dongbeiyujie/textures/entity/dongbeiyujie.png
requirements_addressed:
  - LINK-01
  - RES-02
  - RES-03
autonomous: true
must_haves:
  truths:
    - "D-01: BigSweatyFootItem.postHit no longer applies the Daipai status effect to target entities."
    - "D-02: DaipaiLevelCalculator keeps the +1 final level bonus when Big Sweaty Foot is held in main hand."
    - "D-03: Melee hits with Big Sweaty Foot still play the sound big_sweaty_foot_hit and send attacker-exclusive chat messages."
    - "D-18: The duplicate texture src/main/resources/assets/dongbeiyujie/textures/entity/dongbeiyujie.png is deleted, leaving only dongbei_yujie.png."
  artifacts:
    - path: "src/main/kotlin/com/columbina/yujie/item/BigSweatyFootItem.kt"
      provides: "Post-hit logic without Daipai effect application"
---

<objective>
Remove the Daipai status effect application from the Big Sweaty Foot melee hit logic while preserving the hit sound, attacker-only messages, and +1 level calculation. Clean up the duplicate entity texture.
</objective>

<scope>
In scope:
- Modify `BigSweatyFootItem.postHit` to delete the block of code adding `DAIPAI_ENTRY`.
- Delete the redundant duplicate file `src/main/resources/assets/dongbeiyujie/textures/entity/dongbeiyujie.png`.

Out of scope:
- Do not modify the billboard rendering logic of DongbeiYujieEntity (this uses `dongbei_yujie.png` and remains untouched).
- Do not implement the visual hit particle (this is done in Plan 04).
</scope>

<tasks>
<task type="auto">
  <name>Task 1: Remove status effect application in BigSweatyFootItem</name>
  <files>src/main/kotlin/com/columbina/yujie/item/BigSweatyFootItem.kt</files>
  <action>
    Locate `postHit()` in `BigSweatyFootItem.kt`. Remove the entire block that calls `target.addStatusEffect` with `DAIPAI_ENTRY`. Ensure the sound play logic and random chat message sending logic remain intact.
  </action>
  <verify>
    <automated>./gradlew.bat compileKotlin</automated>
    <automated>powershell -Command "$foot = Get-Content -Raw src/main/kotlin/com/columbina/yujie/item/BigSweatyFootItem.kt; -not ($foot -match 'target\\.addStatusEffect') -and ($foot -match 'target\\.playSound') -and ($foot -match 'attacker\\.sendMessage')"</automated>
  </verify>
  <done>BigSweatyFootItem.postHit no longer applies the Daipai effect.</done>
</task>

<task type="auto">
  <name>Task 2: Delete duplicate entity texture</name>
  <files>src/main/resources/assets/dongbeiyujie/textures/entity/dongbeiyujie.png</files>
  <action>
    Remove the file `src/main/resources/assets/dongbeiyujie/textures/entity/dongbeiyujie.png`. Double check that `src/main/resources/assets/dongbeiyujie/textures/entity/dongbei_yujie.png` is retained and untouched.
  </action>
  <verify>
    <automated>powershell -Command "-not (Test-Path src/main/resources/assets/dongbeiyujie/textures/entity/dongbeiyujie.png) -and (Test-Path src/main/resources/assets/dongbeiyujie/textures/entity/dongbei_yujie.png)"</automated>
  </verify>
  <done>Duplicate entity texture removed.</done>
</task>
</tasks>

<verification>
Run `./gradlew.bat build`. Verify compilation succeeds and `dongbeiyujie.png` is no longer present in the built sources.
</verification>

<success_criteria>
- LINK-01 is partially addressed (melee hit effect removed).
- RES-02 is partially addressed.
- RES-03 is fully addressed.
- D-01, D-02, D-03, and D-18 are respected.
- `./gradlew.bat build` succeeds.
</success_criteria>
