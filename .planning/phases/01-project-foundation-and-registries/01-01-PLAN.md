---
phase: 01
plan: 01
title: Public metadata and template cleanup
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/resources/fabric.mod.json
  - README.md
  - LICENSE
  - src/main/resources/dongbeiyujie.mixins.json
  - src/client/resources/dongbeiyujie.client.mixins.json
  - src/main/java/com/columbina/yujie/mixin/ExampleMixin.java
  - src/client/java/com/columbina/yujie/client/mixin/ExampleClientMixin.java
requirements_addressed:
  - FOUND-01
autonomous: true
must_haves:
  truths:
    - "The packaged mod metadata names Dongbei Yujie Funny Mod instead of the Fabric example template."
    - "The README explains the real project, build command, roadmap, and public-release-safe resource policy."
    - "No required no-op Fabric example mixins are registered or present."
    - "Decision coverage is explicit for public metadata decisions D-01, D-02, D-03, D-04, D-05, D-06, and D-07."
  artifacts:
    - path: "src/main/resources/fabric.mod.json"
      provides: "Real public mod metadata and main entrypoint only"
    - path: "README.md"
      provides: "Public project introduction and licensing/resource notes"
    - path: "LICENSE"
      provides: "GPL-3.0 code license"
  key_links:
    - from: "src/main/resources/fabric.mod.json"
      to: "src/main/kotlin/com/columbina/yujie/DongbeiYujie.kt"
      via: "main Kotlin entrypoint"
      pattern: "com\\.columbina\\.yujie\\.DongbeiYujie"
---

<objective>
Replace the Fabric example public identity with Dongbei Yujie Funny Mod metadata and remove required no-op mixin risk.

Purpose: Phase 1 must make the packaged jar and README identify the actual mod before any gameplay feature work begins.
Output: Release-safe metadata, project README, GPL code license file, and no registered example mixins.
</objective>

<scope>
In scope:
- Update `fabric.mod.json` to describe this project per D-01 through D-06.
- Remove client and datagen entrypoint registration from `fabric.mod.json` per D-11; keep only the `main` entrypoint.
- Remove all `mixins` registration from `fabric.mod.json` and delete the current required no-op mixin descriptors/classes per D-10.
- Replace the Fabric example README per D-07.
- Replace the CC0 template `LICENSE` with GPL-3.0 code licensing per D-05.

Out of scope:
- No item, entity, effect, enchantment, sound, spawn, renderer, datagen provider, PNG, OGG, WAV, MP3, BGM, or voice implementation.
- Do not add later-phase feature IDs such as Big Sweaty Foot, Daipai, or concrete Dongbei Yujie entity registries in this plan.
</scope>

<tasks>
<task type="auto">
  <name>Task 1: Update Fabric mod descriptor</name>
  <files>src/main/resources/fabric.mod.json</files>
  <action>
    Rewrite the descriptor metadata for the real mod. Set `name` to the primary display name from D-01 (`东北雨姐趣味模组`) and make the description include `Dongbei Yujie Funny Mod` as the English/public fallback. Set `authors` to `Castorice1337` per D-02. Set contact references to `ilovecastoriceforever@gmail.com`, `bilibili@MyColumbina`, and source URL `https://github.com/Castorice1337/dongbei-yujie-mod.git` per D-03 and D-04. Set license to `GPL-3.0-only` per D-05. Keep `id`, `${version}`, icon path, environment, `main` Kotlin entrypoint, and dependency versions. Remove `client` and `fabric-datagen` entrypoint blocks per D-11. Remove the `mixins` block entirely per D-10. Replace wildcard `fabric-api` and `fabric-language-kotlin` dependency bounds with tested lower bounds from `gradle.properties`.
  </action>
  <verify>
    <automated>./gradlew.bat build</automated>
    <automated>powershell -Command "(Get-Content -Raw src/main/resources/fabric.mod.json) -notmatch 'FabricMC/fabric-example-mod|This is an example description|Me!|CC0-1.0|dongbeiyujie\\.mixins|dongbeiyujie\\.client\\.mixins|fabric-datagen|DongbeiYujieClient'"</automated>
  </verify>
  <done>`fabric.mod.json` has real project metadata, no Fabric example copy, no client/datagen entrypoints, no mixin registrations, and the build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Replace README and license text</name>
  <files>README.md, LICENSE</files>
  <action>
    Replace the Fabric example README with a concise public-facing project intro per D-06 and D-07. Include the primary display name from D-01, English fallback `Dongbei Yujie Funny Mod`, author/contact/source metadata from D-02 through D-04, current Phase 1 development status, feature roadmap summary, Windows build command `./gradlew.bat build`, and explicit resource copyright notes from D-05 and D-14 stating that final PNG, voice, and BGM assets must be original, licensed, or user-supplied. Replace `LICENSE` with GPL-3.0 code license text or a GPL-3.0-only license notice acceptable for repository root licensing; do not claim bundled media assets are GPL unless they are actually supplied under that license.
  </action>
  <verify>
    <automated>./gradlew.bat build</automated>
    <automated>powershell -Command '$readme = Get-Content -Raw README.md; $license = Get-Content -Raw LICENSE; ($readme -match "Dongbei Yujie Funny Mod") -and ($readme -match "./gradlew\\.bat build") -and ($readme -match "original, licensed, or user-supplied") -and ($readme -notmatch "Fabric Documentation page|template is available under the CC0") -and ($license -match "GNU GENERAL PUBLIC LICENSE|GPL-3.0")'</automated>
  </verify>
  <done>`README.md` presents the mod instead of Fabric template setup text, resource licensing is explicit, `LICENSE` no longer contains CC0 template licensing, and the build succeeds.</done>
</task>

<task type="auto">
  <name>Task 3: Delete no-op mixin scaffold</name>
  <files>src/main/resources/dongbeiyujie.mixins.json, src/client/resources/dongbeiyujie.client.mixins.json, src/main/java/com/columbina/yujie/mixin/ExampleMixin.java, src/client/java/com/columbina/yujie/client/mixin/ExampleClientMixin.java</files>
  <action>
    Delete the two example mixin Java classes and both mixin descriptor JSON files per D-10. Do not replace them with empty descriptors. Future phases may add feature-specific mixins only when Fabric APIs/events cannot handle the behavior.
  </action>
  <verify>
    <automated>./gradlew.bat build</automated>
    <automated>powershell -Command "(-not (Test-Path src/main/java/com/columbina/yujie/mixin/ExampleMixin.java)) -and (-not (Test-Path src/client/java/com/columbina/yujie/client/mixin/ExampleClientMixin.java)) -and (-not (Test-Path src/main/resources/dongbeiyujie.mixins.json)) -and (-not (Test-Path src/client/resources/dongbeiyujie.client.mixins.json))"</automated>
  </verify>
  <done>No required no-op mixin remains in source or resources, and the jar builds without mixin descriptors.</done>
</task>
</tasks>

<verification>
Run `./gradlew.bat build` on Windows after this plan. Confirm `fabric.mod.json`, README, and root license no longer describe the Fabric example template and no longer register client/datagen or mixin entrypoints.
</verification>

<success_criteria>
- FOUND-01 is satisfied.
- D-01 through D-07, D-10, and D-11 are implemented exactly.
- No deferred media/art generation or gameplay registration appears.
- `./gradlew.bat build` succeeds.
</success_criteria>
