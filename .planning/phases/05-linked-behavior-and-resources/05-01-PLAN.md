---
phase: 05
plan: 01
title: Sound registration and placeholder assets
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/kotlin/com/columbina/yujie/registry/DongbeiYujieSounds.kt
  - src/main/resources/assets/dongbeiyujie/sounds.json
  - src/main/resources/assets/dongbeiyujie/sounds/yujie_spawn.ogg
  - src/main/resources/assets/dongbeiyujie/sounds/yujie_idle.ogg
  - src/main/resources/assets/dongbeiyujie/sounds/yujie_attack.ogg
  - src/main/resources/assets/dongbeiyujie/sounds/yujie_hurt.ogg
  - src/main/resources/assets/dongbeiyujie/sounds/yujie_death.ogg
  - src/main/resources/assets/dongbeiyujie/sounds/yujie_bgm.ogg
  - src/main/resources/assets/dongbeiyujie/sounds/daipai_trigger.ogg
  - src/main/kotlin/com/columbina/yujie/entity/DongbeiYujieEntity.kt
requirements_addressed:
  - RES-01
  - RES-02
autonomous: true
must_haves:
  truths:
    - "D-12: Seven new sound events are registered: yujie_spawn, yujie_idle, yujie_attack, yujie_hurt, yujie_death, yujie_bgm, and daipai_trigger."
    - "D-13: The existing big_sweaty_foot_hit is retained, making 8 registered sounds total."
    - "D-14: Minimal silent placeholder OGG files exist for all newly registered sounds to avoid missing file errors."
    - "D-16: All sound and image resources will be packaged inline in the JAR."
    - "D-17: Correct resource paths are registered in code, using silent OGG placeholders for now."
    - "D-48: DongbeiYujieEntity overrides getAmbientSound, getHurtSound, getDeathSound, tryAttack, and plays spawn sound on server side tick."
  artifacts:
    - path: "src/main/kotlin/com/columbina/yujie/registry/DongbeiYujieSounds.kt"
      provides: "Sound registrations"
    - path: "src/main/resources/assets/dongbeiyujie/sounds.json"
      provides: "Sound asset mappings"
---

<objective>
Register new sound events (yujie_spawn, yujie_idle, yujie_attack, yujie_hurt, yujie_death, yujie_bgm, daipai_trigger) and provide silent placeholder OGG files. Connect the first five sounds to the DongbeiYujieEntity behavior.
</objective>

<scope>
In scope:
- Register 7 new sound events in `DongbeiYujieSounds.kt`.
- Reference registered sound events in `sounds.json`.
- Create minimal valid OGG files (silent or very small) in the mod assets for all 7 sounds.
- Override sound-related methods in `DongbeiYujieEntity.kt` (ambient, hurt, death, attack success, and spawn).

Out of scope:
- Do not implement custom BGM instance logic (this is done in Plan 03).
- Do not implement the `daipai_trigger` play logic yet (this is done in Plan 04).
</scope>

<tasks>
<task type="auto">
  <name>Task 1: Register sound events in DongbeiYujieSounds</name>
  <files>src/main/kotlin/com/columbina/yujie/registry/DongbeiYujieSounds.kt</files>
  <action>
    Define ID and SoundEvent constants for yujie_spawn, yujie_idle, yujie_attack, yujie_hurt, yujie_death, yujie_bgm, and daipai_trigger in `DongbeiYujieSounds.kt`. Register all of them in the `register` method using `Registry.register(Registries.SOUND_EVENT, ...)`.
  </action>
  <verify>
    <automated>./gradlew.bat compileKotlin</automated>
  </verify>
  <done>Seven new SoundEvents are registered in DongbeiYujieSounds.</done>
</task>

<task type="auto">
  <name>Task 2: Configure sounds.json and write silent placeholder OGGs</name>
  <files>src/main/resources/assets/dongbeiyujie/sounds.json, src/main/resources/assets/dongbeiyujie/sounds/yujie_spawn.ogg, src/main/resources/assets/dongbeiyujie/sounds/yujie_idle.ogg, src/main/resources/assets/dongbeiyujie/sounds/yujie_attack.ogg, src/main/resources/assets/dongbeiyujie/sounds/yujie_hurt.ogg, src/main/resources/assets/dongbeiyujie/sounds/yujie_death.ogg, src/main/resources/assets/dongbeiyujie/sounds/yujie_bgm.ogg, src/main/resources/assets/dongbeiyujie/sounds/daipai_trigger.ogg</files>
  <action>
    Add sound event mappings to `src/main/resources/assets/dongbeiyujie/sounds.json` for all 7 new sound events. Copy or generate a very small valid silent OGG file to all 7 sound file destinations.
  </action>
  <verify>
    <automated>powershell -Command "Test-Path src/main/resources/assets/dongbeiyujie/sounds/yujie_bgm.ogg"</automated>
  </verify>
  <done>sounds.json updated and silent placeholder files created.</done>
</task>

<task type="auto">
  <name>Task 3: Override sound methods in DongbeiYujieEntity</name>
  <files>src/main/kotlin/com/columbina/yujie/entity/DongbeiYujieEntity.kt</files>
  <action>
    Override `getAmbientSound()`, `getHurtSound()`, and `getDeathSound()` to return `DongbeiYujieSounds.YUJIE_IDLE`, `YUJIE_HURT`, and `YUJIE_DEATH` respectively. Override `tryAttack()` to play `DongbeiYujieSounds.YUJIE_ATTACK` on a successful hit. Add a `playSpawnSound` flag in `tick()` on the server side to play `DongbeiYujieSounds.YUJIE_SPAWN` once when initialized.
  </action>
  <verify>
    <automated>./gradlew.bat compileKotlin</automated>
  </verify>
  <done>DongbeiYujieEntity overrides sound methods and triggers spawn sound.</done>
</task>
</tasks>

<verification>
Run `./gradlew.bat build`. Verify that the project compiles successfully and all OGG placeholders are packaged into the final JAR.
</verification>

<success_criteria>
- RES-01 is fully implemented.
- RES-02 is partially implemented.
- `./gradlew.bat build` succeeds.
- D-12, D-13, D-14, D-16, D-17, and D-48 are respected.
</success_criteria>
