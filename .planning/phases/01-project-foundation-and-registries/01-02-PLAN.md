---
phase: 01
plan: 02
title: Shared registry skeletons
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/kotlin/com/columbina/yujie/DongbeiYujie.kt
  - src/main/kotlin/com/columbina/yujie/DongbeiYujieIds.kt
  - src/main/kotlin/com/columbina/yujie/registry/DongbeiYujieItems.kt
  - src/main/kotlin/com/columbina/yujie/registry/DongbeiYujieEntities.kt
  - src/main/kotlin/com/columbina/yujie/registry/DongbeiYujieEffects.kt
  - src/main/kotlin/com/columbina/yujie/registry/DongbeiYujieEnchantments.kt
  - src/main/kotlin/com/columbina/yujie/registry/DongbeiYujieSounds.kt
  - src/main/kotlin/com/columbina/yujie/world/DongbeiYujieSpawning.kt
requirements_addressed:
  - FOUND-02
autonomous: true
must_haves:
  truths:
    - "Shared registration entrypoints exist for items, entities, effects, enchantments, sounds, and spawning."
    - "The main initializer orchestrates foundation registration without feature logic."
    - "Shared source remains dedicated-server-safe."
    - "Decision coverage is explicit for foundation skeleton decisions D-08, D-09, D-10, D-11, and D-12."
  artifacts:
    - path: "src/main/kotlin/com/columbina/yujie/DongbeiYujie.kt"
      provides: "Main registration orchestration"
    - path: "src/main/kotlin/com/columbina/yujie/registry"
      provides: "Shared registry skeleton package"
    - path: "src/main/kotlin/com/columbina/yujie/world/DongbeiYujieSpawning.kt"
      provides: "Spawn registration anchor"
  key_links:
    - from: "src/main/kotlin/com/columbina/yujie/DongbeiYujie.kt"
      to: "src/main/kotlin/com/columbina/yujie/registry"
      via: "register() calls from onInitialize"
      pattern: "DongbeiYujie.*\\.register"
---

<objective>
Create the shared, dedicated-server-safe registration foundation for later gameplay phases without registering concrete gameplay content.

Purpose: Later phases need stable places for items, entities, effects, enchantments, sounds, and spawn behavior while `DongbeiYujie.onInitialize()` stays orchestration-focused.
Output: Shared registry skeleton objects rooted under `com.columbina.yujie` and wired from the main entrypoint.
</objective>

<scope>
In scope:
- Create a shared namespace helper for the `dongbeiyujie` mod id.
- Create separate shared registry objects for items, entities, effects, enchantments, sounds, and spawning per D-08.
- Wire the registry objects from `DongbeiYujie.onInitialize()`.
- Keep all source under `src/main` dedicated-server-safe per D-12.

Out of scope:
- Do not register concrete Daipai, Big Sweaty Foot, Dongbei Yujie entity, spawn egg, sound events, loot, AI, status effect behavior, enchantment behavior, or spawn event logic per D-09.
- Do not import `net.minecraft.client.*` or reference client renderer/audio classes from shared code.
</scope>

<tasks>
<task type="auto">
  <name>Task 1: Add shared namespace and registry contracts</name>
  <files>src/main/kotlin/com/columbina/yujie/DongbeiYujieIds.kt, src/main/kotlin/com/columbina/yujie/registry/DongbeiYujieItems.kt, src/main/kotlin/com/columbina/yujie/registry/DongbeiYujieEntities.kt, src/main/kotlin/com/columbina/yujie/registry/DongbeiYujieEffects.kt, src/main/kotlin/com/columbina/yujie/registry/DongbeiYujieEnchantments.kt, src/main/kotlin/com/columbina/yujie/registry/DongbeiYujieSounds.kt</files>
  <action>
    Create `DongbeiYujieIds` with `const val MOD_ID = "dongbeiyujie"` and an identifier helper using current Yarn/Minecraft 1.21.11 APIs. Create one Kotlin `object` per registry area: `DongbeiYujieItems`, `DongbeiYujieEntities`, `DongbeiYujieEffects`, `DongbeiYujieEnchantments`, and `DongbeiYujieSounds`. Each object should expose a clearly named registration entry method such as `register()` and contain only the structure needed for future `Registry.register` calls. Add short comments that identify the later-phase owner of concrete registrations, but do not add concrete IDs or placeholder registrations. Use D-08 for structure and D-09 for the no-gameplay boundary.
  </action>
  <verify>
    <automated>./gradlew.bat build</automated>
    <automated>powershell -Command '$paths = @("src/main/kotlin/com/columbina/yujie/DongbeiYujieIds.kt", "src/main/kotlin/com/columbina/yujie/registry/DongbeiYujieItems.kt", "src/main/kotlin/com/columbina/yujie/registry/DongbeiYujieEntities.kt", "src/main/kotlin/com/columbina/yujie/registry/DongbeiYujieEffects.kt", "src/main/kotlin/com/columbina/yujie/registry/DongbeiYujieEnchantments.kt", "src/main/kotlin/com/columbina/yujie/registry/DongbeiYujieSounds.kt"); ($paths | Where-Object { -not (Test-Path $_) }).Count -eq 0'</automated>
  </verify>
  <done>Shared namespace helper and five registry skeleton objects exist, compile, and contain no concrete gameplay registrations.</done>
</task>

<task type="auto">
  <name>Task 2: Add spawn registration skeleton</name>
  <files>src/main/kotlin/com/columbina/yujie/world/DongbeiYujieSpawning.kt</files>
  <action>
    Create `DongbeiYujieSpawning` under a shared/server-safe package such as `com.columbina.yujie.world`. Provide a `register()` entry method for future controlled night spawning setup per D-08. Keep it structural only: no event callbacks, spawn constants, entity lookups, player scanning, biome modifications, or concrete Yujie spawn rules in Phase 1 per D-09.
  </action>
  <verify>
    <automated>./gradlew.bat build</automated>
    <automated>powershell -Command "Test-Path src/main/kotlin/com/columbina/yujie/world/DongbeiYujieSpawning.kt"</automated>
  </verify>
  <done>Shared spawn registration anchor exists and compiles without implementing night spawning behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Wire shared registration from main entrypoint</name>
  <files>src/main/kotlin/com/columbina/yujie/DongbeiYujie.kt</files>
  <action>
    Replace the Fabric template startup comment and `Hello Fabric world!` log with Dongbei Yujie foundation initialization. Keep the private SLF4J logger pattern. In `onInitialize()`, call each shared registration entry method in a stable order: items, entities, effects, enchantments, sounds, spawning. The entrypoint should only orchestrate registration and log a concise lifecycle message. Do not put feature logic directly in `DongbeiYujie.kt`.
  </action>
  <verify>
    <automated>./gradlew.bat build</automated>
    <automated>powershell -Command '$main = Get-Content -Raw src/main/kotlin/com/columbina/yujie/DongbeiYujie.kt; ($main -match "DongbeiYujieItems\\.register") -and ($main -match "DongbeiYujieEntities\\.register") -and ($main -match "DongbeiYujieEffects\\.register") -and ($main -match "DongbeiYujieEnchantments\\.register") -and ($main -match "DongbeiYujieSounds\\.register") -and ($main -match "DongbeiYujieSpawning\\.register") -and ($main -notmatch "Hello Fabric world")'</automated>
    <automated>powershell -Command '$mainSources = Get-ChildItem src/main -Recurse -Include *.kt,*.java | ForEach-Object { Get-Content $_.FullName }; -not ($mainSources -match "net\\.minecraft\\.client\\.")'</automated>
  </verify>
  <done>Main initialization is real project orchestration, all shared registry skeletons are called, no client imports exist under `src/main`, and the build succeeds.</done>
</task>
</tasks>

<verification>
Run `./gradlew.bat build`. Then confirm shared registry files exist under `src/main`, `DongbeiYujie.onInitialize()` calls every shared registration object, and there are no `net.minecraft.client.*` imports under `src/main`.
</verification>

<success_criteria>
- FOUND-02 is satisfied.
- D-08, D-09, and D-12 are respected.
- Entry points remain orchestration-focused.
- No concrete gameplay content appears before later phases.
- `./gradlew.bat build` succeeds.
</success_criteria>
